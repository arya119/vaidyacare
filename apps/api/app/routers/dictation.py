"""Router: dictation — API_SPEC.md stub. Role 4 fills endpoints in W1."""
from fastapi import APIRouter

router = APIRouter(tags=["dictation"])

# TODO: implement endpoints per API_SPEC.md §dictation
