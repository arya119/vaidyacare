"""Router: imbalance — API_SPEC.md stub. Role 4 fills endpoints in W1."""
from fastapi import APIRouter

router = APIRouter(tags=["imbalance"])

# TODO: implement endpoints per API_SPEC.md §imbalance
